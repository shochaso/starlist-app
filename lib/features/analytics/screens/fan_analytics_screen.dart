import 'package:flutter/material.dart';

class FanAnalyticsScreen extends StatelessWidget {
  const FanAnalyticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'ファン分析画面 (実装予定)',
        style: TextStyle(color: Colors.white, fontSize: 24),
      ),
    );
  }
}
