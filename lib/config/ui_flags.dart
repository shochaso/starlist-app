/// UI feature flags related to import flows.
///
/// When `kHideImportImages` is true the data import experience must avoid
/// rendering any image-based widgets, producing text-only layouts instead.
const bool kHideImportImages = true;
