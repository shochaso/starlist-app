@Skip('domain models are pending migration into lib/src/models')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('models test suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
