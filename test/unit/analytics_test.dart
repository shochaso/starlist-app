@Skip(
    'analytics suite depends on starlist_implementation and is disabled for now')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('analytics suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
