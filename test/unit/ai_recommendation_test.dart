@Skip('AI recommendation prototypes live in starlist_implementation package')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('ai recommendation suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
