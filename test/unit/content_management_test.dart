@Skip('content management suite relies on starlist_implementation package')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('content management suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
