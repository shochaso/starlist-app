@Skip(
    'affiliate product domain models are tracked in starlist_improved package')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('affiliate product suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
