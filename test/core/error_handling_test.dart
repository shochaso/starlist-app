@Skip('legacy error handling tests; pending migration')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('error handling suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
