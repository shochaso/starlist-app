@Skip('legacy performance tests; pending migration')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('performance suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
