@Skip('legacy monetization suite; pending migration')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('monetization suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
