@Skip('feed/content stack pending re-home')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('virtualized feed suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
