@Skip('legacy subscription suite; pending migration')
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('subscription suite temporarily skipped', () {
    expect(true, isTrue);
  });
}
