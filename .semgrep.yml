<<<<<<< HEAD
# STARLIST Semgrep configuration (extends p/ci ruleset)
# Used together with CLI-level directory exclusions in the Extended Security workflow.
# Placeholder rule ensures the configuration file remains valid while we iterate on per-rule tuning.
rules:
  - id: starlist-placeholder-noop
    languages: [generic]
    message: "Placeholder rule for repository-specific Semgrep configuration."
    severity: INFO
    patterns:
      - pattern: $X
      - metavariable-pattern:
          metavariable: $X
          pattern: "(?!)"  # impossible regex to avoid matches
=======
rules:
  - id: no-hardcoded-secret
    patterns:
      - pattern: /(?i)(api_key|secret|token)\s*[:=]\s*["'][A-Za-z0-9_\-]{12,}["']/
    message: Avoid embedding secrets in source.
    severity: ERROR
    languages: [generic]
  - id: deno-fetch-no-http
    pattern: 'fetch("http://$X", ...)'
    message: Use HTTPS for outbound fetch requests.
    severity: WARNING
    languages: [typescript, javascript]
>>>>>>> 8abb626 (feat(ops): add ultra pack enhancements — Makefile, audit bundle, risk register, RACI matrix)
